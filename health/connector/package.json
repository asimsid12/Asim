{
  "name": "google-health-connector",
  "version": "1.0.0",
  "description": "Remote MCP server exposing Google Health data to Claude as a custom connector",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
